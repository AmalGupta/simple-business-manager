{
  "name": "simple-business-manager",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "wrangler dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "deploy": "pnpm build && wrangler deploy",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "eval": "tsx packages/core/prompts/evals/run.ts",
    "d1:migrate:local": "wrangler d1 migrations apply sbm-dev --local",
    "d1:migrate:remote": "wrangler d1 migrations apply sbm-dev --remote"
  },
  "dependencies": {
    "@sbm/core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
