# Copy to web/.env (gitignored). Must match the deployed SBM_API_KEY secret
# (wrangler secret put SBM_API_KEY) — baked into the built bundle at build
# time since the dashboard is a static SPA with no login step.
VITE_SBM_API_KEY=replace-with-the-same-value-as-the-SBM_API_KEY-secret
